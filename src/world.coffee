_ = require 'lodash'
History = require './history.coffee'

module.exports = class World
  constructor: ->
    @t = 0 # current time in simulation
    #TODO
  step: ({t, tMin, tMax}) ->
    null
    #TODO